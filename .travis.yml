dist: xenial
sudo: required
language: node_js
node_js:
  - "16.13.1"
matrix:
  include:
    - os: linux
      dist: xenial
install:
  - bash ./bin/travis-install.sh
script:
  - bash ./bin/build.sh
  - truffle run coverage --network development
after_script:
 - bash <(curl -s https://codecov.io/bash)
